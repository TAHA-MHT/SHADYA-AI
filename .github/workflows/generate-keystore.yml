name: Generate Keystore

on:
  workflow_dispatch:

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - name: Generate keystore
        run: |
          keytool -genkeypair -v -keystore release.keystore \
            -alias dccgeotrack \
            -keyalg RSA -keysize 2048 -validity 10000 \
            -storepass "ChangeMe123!" \
            -keypass "ChangeMe123!" \
            -dname "CN=DCC GeoTrack, OU=Mobile, O=DCC, L=NDjamena, S=Chad, C=TD"
          base64 -w0 release.keystore > release.keystore.b64

      - name: Display keystore base64
        run: cat release.keystore.b64

      - name: Generate random passwords
        run: |
          echo "RELEASE_KEYSTORE_PASSWORD=ChangeMe123!"
          echo "RELEASE_KEY_PASSWORD=ChangeMe123!"
          echo "RELEASE_KEY_ALIAS=dccgeotrack"

      - name: Upload keystore
        uses: actions/upload-artifact@v4
        with:
          name: keystore-files
          path: |
            release.keystore
            release.keystore.b64
